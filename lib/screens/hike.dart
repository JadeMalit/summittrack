import 'package:flutter/material.dart';

class HikeScreen extends StatelessWidget {
  const HikeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Hike Screen'),
      ),
    );
  }
}
