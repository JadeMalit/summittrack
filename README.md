# SummitTrack

SummitTrack is organized as a single repository containing:

- `SummitTrack-Front-End/` — Flutter application
- `SummitTrack-Back-End/` — Node.js/Express service

Local dependency folders, generated build output, environment files, and
workspace backups are excluded from version control.
